import React from 'react';
import RequirementsList from '../components/checklist/RequirementsList';

const RequirementsPage: React.FC = () => {
  return (
    <div className="min-h-full">
      <RequirementsList />
    </div>
  );
};

export default RequirementsPage;
